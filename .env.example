# URL del backend (sin barra al final)
# Desarrollo: usa el proxy de Vite, esta variable NO es necesaria en dev
# Producción: la URL de tu servicio en Render + /api
VITE_API_URL=https://tu-servicio.onrender.com/api
